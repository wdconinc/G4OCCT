# SPDX-License-Identifier: LGPL-2.1-or-later
# Copyright (C) 2026 G4OCCT Contributors

schema_version: geometry-fixture-provenance/v1
fixture_id: g4vtwistedfaceted-faceted-dz20-theta8-phi20-v1
generator:
  tool: DRAWEXE
  script: generate.tcl
shape:
  geant4_class: G4VTwistedFaceted
  constructor:
    dz_mm: 20.0
    phi_twist_deg: 30.0
    theta_deg: 8.0
    phi_deg: 20.0
    dy1_mm: 8.0
    dx1_mm: 9.0
    dx2_mm: 12.0
    dy2_mm: 10.0
    dx3_mm: 7.0
    dx4_mm: 13.0
    alpha_deg: 12.0
validation:
  # Analytical G4 formula: ((dx1+dx2+dx3+dx4)*(dy1+dy2) + (dx4+dx3-dx2-dx1)*(dy2-dy1)/3)*dz
  #   = ((9+12+7+13)*(8+10) + (13+7-12-9)*(10-8)/3)*20 = 14746.667 mm³
  # 64-section tessellated solid (flat triangular faces, 508 total): 14702.1 mm³ (0.3% below analytical, expected for coarse tessellation)
  expected_volume_mm3: 14702.1
  # G4VTwistedFaceted::DistanceToIn/DistanceToOut have known systematic inaccuracies
  # (errors of 1–5 mm for these parameters).  The OCCT ruled-loft solid is geometrically
  # correct (analytically verified at z=0: x-extent = ±10.25 mm, y-extent = ±9.0 mm).
  # Ray-distance comparisons are expected to fail; see fixture_validation.cc xfail.
  distance_tolerance_mm: 1.0e-3
